# Python
__pycache__/
*.pyc
.env
.venv/
venv/

# Node
node_modules/
dist/

# Frontend Data — regenerated by export_frontend.py, and TRACKED since 2026-08-06.
# They were ignored while the site only ever ran from a dev machine; a static host builds from this
# repo, so ignoring them publishes 180 dead links. The live payloads do expire weekly — that is
# handled where it belongs, by the `vencido` state and the refresh job that recommits them, not by
# withholding the file.

# Agent tooling & working context — personal aids, not part of the project
/.agents/
/.claude/
/.interface-design/
/context/

# Data is local-only — too large for git. Paths documented in context/sources/index.md
data/raw/
data/processed/

# Generated EDA reports — regenerate from notebooks/03_eda_comprehensive.ipynb
notebooks/*_EDA_Report.html

# Agent instructions — local only for now
CLAUDE.md
