# Python
__pycache__/
*.pyc
.env
.venv/
venv/

# Node
node_modules/
dist/

# Frontend Data — regenerated by export_frontend.py. The index stays tracked; the per-facility
# files do not, and the live ones expire weekly, so tracking them would commit a stale forecast.
frontend/public/data/facility/
frontend/public/data/live/

# Agent tooling & working context — personal aids, not part of the project
/.agents/
/.claude/
/.interface-design/
/context/

# Data is local-only — too large for git. Paths documented in context/sources/index.md
data/raw/
data/processed/

# Generated EDA reports — regenerate from notebooks/03_eda_comprehensive.ipynb
notebooks/*_EDA_Report.html

# Agent instructions — local only for now
CLAUDE.md
